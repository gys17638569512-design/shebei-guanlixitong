# 智能起重机维保管理系统 - 项目搭建记录

## 项目概述
- **项目名称**：智能起重机维保管理系统
- **技术栈**：
  - 后端：FastAPI + SQLAlchemy + SQLite
  - 前端：Vue 3 + Element Plus + Pinia + Vue Router

## 搭建过程

### 1. 后端服务搭建
- **时间**：2026-03-09
- **状态**：已完成
- **内容**：
  - 创建了完整的后端目录结构
  - 实现了认证系统、工单管理、文件上传、PDF报告生成、e签宝集成等功能
  - 初始化了测试数据
  - 服务运行在 http://localhost:8001/ (原 8000 端口冲突，已切换)

### 2. 前端服务搭建
- **时间**：2026-03-09
- **状态**：已完成
- **内容**：
  - 创建了 Vue 3 项目
  - 安装了 Element Plus、Axios、Pinia、Vue Router 等依赖
  - 实现了登录页面、工单列表页面、布局组件等
  - 服务运行在 http://localhost:5173/

## 遇到的问题及解决方案

### 1. 数据库连接问题
- **问题**：MySQL 连接失败
- **解决方案**：改用 SQLite 数据库，简化配置

### 2. 密码长度问题
- **问题**：bcrypt 密码长度限制
- **解决方案**：使用预生成的密码哈希值

### 3. 前端依赖问题
- **问题**：缺少 Pinia 和 Vue Router 依赖
- **解决方案**：安装相应依赖包

### 4. 目录结构问题
- **问题**：缺少前端视图目录
- **解决方案**：创建缺失的目录结构和视图文件

### 5. 前端依赖问题
- **问题**：缺少 Pinia 和 Vue Router 依赖
- **解决方案**：安装相应依赖包

### 6. Vite 缓存问题
- **问题**：Vite 无法识别新创建的文件
- **解决方案**：重启前端服务

### 7. 登录接口路由问题
- **问题**：登录接口返回 404 错误
- **解决方案**：统一配置路由前缀，确保登录接口路径正确

### 8. 数据库模型循环引用问题
- **问题**：WorkOrder 模型引用 Customer 模型时出现循环引用错误
- **解决方案**：调整模型导入顺序，确保所有模型都被正确注册到 Base 中

### 9. bcrypt 密码哈希问题
- **问题**：bcrypt 库出现版本读取错误，导致密码验证失败
- **解决方案**：暂时使用明文密码验证，绕过 bcrypt 库的问题

### 10. 数据库初始化问题
- **问题**：seed_data.py 脚本执行失败，无法插入用户数据
- **解决方案**：修改 seed_data.py 脚本，直接插入用户数据，绕过密码哈希步骤

### 3. 后端架构重构与规范化 (2026-03-09)
- **状态**：已完成
- **内容**：
  - 修复登录绕过与 bcrypt 安装问题，恢复安全的哈希验证密码机制。
  - 创建了 `core/response.py` 与 `core/exceptions.py` 实现后端全局统一的 JSON 相应格式 (`{"code": 200, "data": ...}`) 以及异常拦截。
  - 创建了 `core/audit.py` 和关联的模型，实现了数据库全量操作的日志审计。
  - 对核心的路由文件（`auth.py`, `order.py` 乃至所有模块）进行了重构，抽离了底层的数据库查询操作至专门的 `repositories/` 和业务层 `services/`，严格遵循单向调用规范。
  - 对 FastAPI 接口文档中的 Tags 与 Summary 进行了全面的汉化，方便后续维护使用。

### 4. PC 管理后台 Vue 深入开发 (2026-03-09)
- **状态**：已完成
- **内容**：
  - 修改了 `request.js` 全局响应拦截器，使其能自动解包适应新的接口封装。
  - 修复了 vite 构建配置中 `@` 路径别名丢失导致的模块解析失败问题。
  - 研发并上线了五个核心的 PC 端管理页面视图：
    - `CustomerList.vue`: 客户列表以及动态添加额外联系人的侧边抽屉。
    - `CustomerDetail.vue`: 客户详情资料与下属设备的快速查阅组件。
    - `EquipmentForm.vue`: 设备档案录入表单，集成了基于大类与型式智能生成部件清单的联动交互。
    - `OrderList.vue`: 工单中心，用于使用状态标签卡片流览分派记录，带弹窗创建工单功能。
    - `OrderDetail.vue`: 工单流转详情，可查阅电子签名及瀑布流展示现场照片记录。

## 遇到的问题及解决方案

### 11. 前端 Vite 别名解析失败白屏
- **问题**：`import ... from '@/...'` 导入报错找不到模块。
- **解决方案**：在 `vite.config.js` 中引入 `fileURLToPath` 手动配置 `resolve.alias` 指向 `src` 目录。

### 12. 老旧后端路由高耦合
- **问题**：早期的路由设计在接口层直接执行 `db.query` 带来长期的难以维护性。
- **解决方案**：一次性规范化建立 `services/` 与 `repositories/` 包结构，解耦执行逻辑。

## 已完成的工作
- 测试完整的前后端交互
- 修复了登录功能，确保用户可以正常登录系统
- 验证了登录接口可以正常返回访问令牌
- 彻底清理和重构了后端服务符合单向隔离规范
- 跑通了涵盖客户、设备、工单管理的核心 PC 界面

## 待完成的工作
- 完善前端功能
- 优化系统性能
- 增加更多测试用例

## 测试账号
- **管理员**：admin / Admin@2024
- **经理**：manager01 / Manager@2024
- **工程师**：tech01 / Tech@2024